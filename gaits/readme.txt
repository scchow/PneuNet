#Information
# 1st comment "x increments y z sweep" indicates x = seconds per cycle, y = valve open percentage (1.0 = full open, 0.0 = close)
# |1. Which time increment should it open | 2. How many increments (length it is open)| 3. multiplier for duty cycle , <repeat for next columns>
# each row is a channel (4 max for now)